class Rectangle {
    init(h, w) {
        super.init();
        this.h = h;
        this.w = w;
    }

    area() {
        return this.h * this.w;
    }
}

class Square < Rectangle {
    init(h) {
        super.init(h, h);
    }
}

var r = Rectangle(2, 4);
var s = Square(4);

print r.area();
print s.area();

